### Added

- Subtle cursor parallax: the cloud tilts a few degrees toward the pointer, layered on the idle orbit. (#44)
- A faint date (z) axis line through the cloud centre as a depth reference. (#44)

### Changed

- Slightly faster idle auto-rotation and a touch more aerial depth-fog so the 3D reads at a glance (kept subtle). (#44)
- The source/axis legend fades out while the detail panel is open, so the panel never covers it on narrow screens. (#42)
