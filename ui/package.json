{
  "name": "paperverse-ui",
  "private": true,
  "type": "module",
  "description": "Paperverse L4 UI (Three.js + sql.js). Not shipped in the Python wheel.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
